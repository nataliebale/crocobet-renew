export interface SlotGame {
  enabled: boolean;
  // eslint-disable-next-line camelcase
  game_id: string;
  image: string;
  name: string;
  order: number;
  provider: string;
  tags: string[];
  url: string;
  _id: string;
}
